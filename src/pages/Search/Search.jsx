import React from 'react';

import s from './search.scss';

function Search() {
    return (
        <section>
            <p className={s.title}>Search</p>
        </section>
    );
};

export default Search;