const likeAnim = (function() {
    let anim = (className) => {
    }
    
    return {
        anim: function(className) {
            anim(className);
        }
    }
})();

module.exports = likeAnim;